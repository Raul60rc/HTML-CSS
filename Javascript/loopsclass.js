// A loop that starts at 30, minuses numbers 2 in 2, so we end up having even numbers & the limit should be -10
for (let i= 30; index > -10; index-2) {
   console.log("30 to -10")
    
}
// A loop that starts in 100, goes dividing in 2 till it goes below 10
function divide(dividend,divisor) {
    var result = 0
    while(dividend >= divisor){
        dividend
    }

    console.log(divide)
    
}
// A loop that starts -40, should multiply *-2 until it reaches to 1000
for (let index = -40; index < 1000; index*2) {
   console.log("print till 1000")
    
}
// a piramide of * star sign. 
let piramid = "*"

for (let i = 2; i < 12; i++) {
    for (let j = 1; j < i )
    
}


//Example to understand loops with the folowing code 
let piramide = '';
for (let i = 2; i <= 7; i++) {
    console.log(`Estoy en la vuelta ${i-1} del bucle exterior`);
  for (let j = 1; j < i; j++) {
    console.log(`    Estoy en la vuelta ${j} del bucle interior mi límite es ${i}`);
  }
    piramide += '\n';
  }
  console.log(piramide);

  //Escribir una funcion random
  function comprarRegalos(){
    console.log('comprar regalo');
    console.log('libro comprado');
  }
  
